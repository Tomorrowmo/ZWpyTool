# 对话式改模助手 · Demo 方案

> 一句话：**打开一个从 Creo 导进来的工装模型，用中文说一句话，模型就改好了。**

---

## 一、为什么是这个 Demo

客户的原话是"**在历史 CAD 数据基础之上，用中望结合 AI 进行二次设计**"。

拆开看有两条路：

| | 做法 | 判断 |
|---|---|---|
| A | 把 Creo 历史模型转过来，**改参数**让它重新生成 | ❌ **不可行**。转换后是哑几何，无特征树；中望 API 内**无特征识别**（`Recogni` 关键词在 4,296 个函数中零命中） |
| B | 把 Creo 历史模型转过来，**直接编辑**局部（改尺寸/加孔/去孔） | ✅ **可行**。直接编辑技术存在的理由就是编辑无历史模型 |

**B 就是客户真正想要的**，而且哑几何 + 直接编辑是天生一对。这个 Demo 直接命中需求，且是全链路里技术风险最低的一段。

---

## 二、Demo 形态

中望里挂一个侧边栏面板（ZW3D UI Designer 做的 Form）：

```
┌──────────────────────────────┬────────────────────────┐
│                              │  对话式改模            │
│                              │ ─────────────────────  │
│      【 3D 模型视区 】        │  ┌──────────────────┐  │
│                              │  │ 把四个角的安装孔 │  │
│   AI 认定的目标面高亮显示     │  │ 从 Φ10 改成 Φ12  │  │
│   （ZwFaceDisplayAttributeSet│  └──────────────────┘  │
│                              │         [ 解析 ]       │
│                              │ ─────────────────────  │
│                              │  我理解你要改：        │
│                              │  ☑ 底板-角孔①  Φ10→12 │
│                              │  ☑ 底板-角孔②  Φ10→12 │
│                              │  ☑ 底板-角孔③  Φ10→12 │
│                              │  ☑ 底板-角孔④  Φ10→12 │
│                              │                        │
│                              │  ⚠ 校验：孔边距 8mm，  │
│                              │    改后 7mm，仍满足    │
│                              │                        │
│                              │   [ 执行 ]  [ 撤销 ]   │
└──────────────────────────────┴────────────────────────┘
```

**三条交互铁律**（缺一个 Demo 就翻车）：

1. **先高亮，后执行。** AI 解析完必须在模型上高亮 + 列出清单，用户点确认才动手。**绝不允许一句话直接改模型。**
2. **每步可撤销。** 每次直接编辑生成一个独立特征，用 `ZwHistoryOperationDelete` 单步回退。
3. **改错了要能说清。** 校验层的结论要显示出来（边距、壁厚、干涉），不是黑箱。

---

## 三、演示脚本（用力学试验工装实例）

模型：方形底座 + 环形凹槽 + 2 个顶部压块 + 2 个侧压板 + 4 个角块。**从客户自己的 Creo 文件导入。**

| # | 说的话 | 调用 | 看点 |
|---|---|---|---|
| 1 | "把底板四个角的安装孔从 Φ10 改成 Φ12" | `cvxPartModifyHole` | **一句话改一组孔**，AI 自己认出"四个角"是同一组 |
| 2 | "在两个压块上各加一个 M8 螺纹孔，距边 15" | `cvxPartHole` | 加孔 + 螺纹规格从 `Thread.xml` 取标准值 |
| 3 | "把环槽旁边那两个定位销孔去掉" | `cvxPartSimplify` | **删面愈合**，最有冲击力的一条 |
| 4 | "环形凹槽内径加大 2mm" | `cvxPartDERad` | 改圆柱面半径 |
| 5 | "侧压板厚度从 12 加到 15" | `cvxPartDEDimMove` | **随动面自动跟随**（相邻圆角、孔壁），技术含量最高的一条 |
| 6 | "刚才那步撤销" | `ZwHistoryOperationDelete` | 建立信任感 |
| 7 | （可选）"出一张三视图带标注和明细表" | `ZwDrawingDimensionAutoCreate`<br>`ZwDrawingAutoBalloonCreate`<br>`ZwDrawingTableBOMCreate` | **自动出图，压轴** |

> 第 7 条是加分项。工装设计里出图常占 40% 工时，而中望这块接口出乎意料地强（标注 98 个函数 + 视图 86 个函数）。**如果时间允许，一定要演。**

---

## 四、技术架构

```
┌─ Python 侧（AI）────────────────────────────┐
│  LLM：自然语言 → 结构化编辑指令 JSON        │
│  语义索引：孔/面 → 人类可读标签             │
│  指代消解："左边那个" → 具体 face id        │
└──────────────┬──────────────────────────────┘
               │  本地 HTTP / 命名管道（需自研，中望无 Python 绑定）
┌──────────────▼──────────────────────────────┐
│─ C++ 侧（ZW3D 插件 DLL）────────────────────│
│  语义提取：cvxPartInqHoles + ZwFace*        │
│  规则校验：边距/壁厚/螺纹标准/干涉（确定性）│
│  执行：cvxPartModifyHole / Hole / Simplify  │
│        cvxPartDE{DimMove,Rad,Offset,Fllt}   │
│  预览：ZwFaceDisplayAttributeSet            │
│  回退：ZwHistoryOperationDelete             │
│  UI：ZW3D UI Designer Form                  │
└─────────────────────────────────────────────┘
```

**分工原则：AI 决定"改什么"，确定性代码决定"能不能改、怎么改"。**
校验层绝不交给 AI——工装出了问题要能追责。

---

## 五、核心技术点：三个难，一个易

### 🟢 易 —— 语义提取几乎是白送的

`cvxPartInqHoles` 一次返回模型上**全部孔**，且 `svxHoleData` 里带着：

```
Standard(执行标准) + Screw(螺旋类型) + Size(尺寸) + Fit(配合方式)
```

四个字段拼起来就是一句人话："**GB 内六角螺钉 M8 Normal 配合**"。
加上 `Diameter1/Depth1/Diameter2/Depth2`（孔+沉孔）、`Points[]`（位置）、`Callout[128]`（编号标签）——

> **"让 AI 看懂模型"这个最难的问题，被 API 解决了一大半。** 这是这个 Demo 能在 3~4 周做出来的根本原因。

### 🔴 难点 1 —— 指代消解（真正的核心）

"**左边那个孔**""**这个定位销孔**"→ 具体 face id。

**解法：**
- **几何语义标签**：按位置 + 属性聚类命名 → `底板-四角安装孔组(4个·Φ10沉孔·Z=0底面)`
- **AI 输出标签，不输出 id**
- **交互确认兜底**——不追求一次说对，追求说错了看得见、改得回

> 这层"几何 → 人话"的映射是**产品真正的壁垒**，也是别人抄不走的部分。

### 🔴 难点 2 —— 面 id 失效

每次直接编辑后拓扑改变，面 id 全部作废。
**解法**：架构上按"**每轮操作后重建索引**"设计，绝不缓存 id。（不是难题，但一开始不这么设计后期会返工。）

### 🔴 难点 3 —— 哑几何无约束联动

"侧压板从 12 加到 15"，直接编辑只移动一个面，**相邻的圆角、倒角、孔壁不会跟着走**，模型会撕裂。

**解法**：`svxDEDimMove.idEnts` 随动面列表 + `ZwFaceConnectedFaceGet` 自动推算"相关面组"。
**这是整个 Demo 技术含量最高的地方**，也是演示脚本第 5 条的看点。

---

## 六、工期与分工

**一个 C++ 开发 + 一个 AI/Python 开发，3~4 周**（前提：试用 license 已到位）

| 周 | 内容 | 产出 |
|---|---|---|
| W1 | `ZwFileProeImport` 打通 + 语义提取（孔 + 面）→ JSON | 能把客户的 Creo 工装读进来，导出语义清单 |
| W2 | 执行层：改孔 / 加孔 / 去孔 / DERad / DEDimMove + 随动面推算 | 命令行能驱动 6 类编辑 |
| W3 | UI Form + AI 桥接 + 高亮预览 + 撤销 | 端到端跑通 |
| W4 | 规则校验层 + 调试打磨 + 演示彩排 | 可演示 |

**W1 是关卡**：如果 `ZwFileProeImport` 批量导入有弹窗、或装配层级散架，方案要调整。**第一周就要把这个风险出清。**

---

## 七、验收标准

- [ ] 从客户**真实 Creo 工装文件**导入成功，装配层级完整
- [ ] 语义清单能列出全部孔，含直径/深度/位置/标准/配合
- [ ] 演示脚本 6 条指令全部一次通过
- [ ] 每步可高亮预览、可撤销
- [ ] 改后模型可通过干涉检查
- [ ] 全程无崩溃、无内存泄漏（连续操作 50 次）

---

## 八、Demo 之后的路线

| | 阶段 | 内容 | 价值 |
|---|---|---|---|
| **1** | **对话式改模** | ← 本 Demo | 入口。最快出效果，风险最低 |
| 2 | **规则校验做厚** | 边距、壁厚、螺纹标准、材料、干涉、企业设计规范 | **真正的护城河**。从"能改"到"改得对"，规则一旦编进去客户换不掉 |
| 3 | 历史库资产化 + 相似检索 | 批量提取全部历史工装 → 结构化台账 → 新产品来了找相似案例 | 数据资产，越用越准 |
| 4 | 参数化模板 + 全自动生成 | 从历史数据归纳 N 类范式，做成原生参数化模板 | 从"改现成的"到"生成新的" |
| 5 | **CAE 校核闭环** | 参数生成 → 建模 → 网格/边界条件 → 强度校核 → 不过自动调参 | **差异化。力学工装本来就要校核，纯 CAD 集成商做不了** |

**定位不是"我们帮你把 CAD 自动化"（任何集成商都这么说），而是"带校核闭环的自动设计"——AI 敢改参数，是因为后面有校核兜底。**

---

## 九、待确认事项（不影响 Demo 启动，但影响产品形态）

| # | 问题 | 影响 |
|---|---|---|
| 1 | **ZW3D 是否支持 headless / 批处理**（chm 中 `Batch`/`Silent` 零命中） | 决定是"桌面插件"还是"服务端批量生成"。**最关键** |
| 2 | API 开发者授权 + 插件分发授权政策 | 商务模型 |
| 3 | Qt 商业授权边界（《API 简介》P28 的限制） | UI 做多复杂 |
| 4 | `ZwFileProeImport` 是否保留装配结构/属性、能否无人值守批量跑 | W1 自测即可得出 |
| 5 | 新接口何时补齐装配约束等（现仅 `ZwConstraintCoincidentAdd`） | 阶段 4 的技术债 |

> 前 3 项 chm 答不了，需向中望（`zdncentre@zwsoft.com`）确认。第 4 项自己一周内能测出来。

---

## 十、这个 Demo 为什么会赢

1. **客户当场能上手试**——不是看 PPT，是拿他们自己的模型改
2. **直接命中原话**——"在历史数据基础上二次设计"，一句话对上
3. **技术风险最低**——绕开了特征识别这个死结，走的是成熟的直接编辑
4. **门槛看起来低、壁垒实际高**——语义层和规则层是慢慢攒出来的，抄不走
5. **不跟中望绑死**——底座是"CAD 历史数据 + AI 辅助设计"，换 SolidWorks/UG/CATIA 都成立，`ZwFileProeImport` 只是一个数据入口
